#!/usr/bin/make -f

%:
	dh $@

override_dh_install:
	dh_install
	install -D -m 755 script/homelab-cli \
		debian/homelab-cli/usr/bin/homelab-cli

override_dh_auto_build:
	# No build step needed for a single Python script

override_dh_auto_test:
	# No package-build-time tests
