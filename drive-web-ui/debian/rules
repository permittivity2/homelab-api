#!/usr/bin/make -f

%:
	dh $@ --buildsystem=perl_makemaker

override_dh_auto_test:

override_dh_install:
	dh_install
	# Systemd service unit
	install -D -m 644 systemd/homelab-drive-web-ui.service \
		debian/homelab-drive-web-ui/etc/systemd/system/homelab-drive-web-ui.service
	# Templates and public assets — installed to /usr/share/homelab-drive-web-ui/
	find templates -type f -exec install -D -m 644 {} \
		debian/homelab-drive-web-ui/usr/share/homelab-drive-web-ui/{} \;
	find public -type f -exec install -D -m 644 {} \
		debian/homelab-drive-web-ui/usr/share/homelab-drive-web-ui/{} \;
	find config -type f -exec install -D -m 644 {} \
		debian/homelab-drive-web-ui/usr/share/homelab-drive-web-ui/{} \;
